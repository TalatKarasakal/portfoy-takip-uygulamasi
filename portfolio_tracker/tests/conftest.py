import pytest
from app.models.price_history import PriceHistory
from app.models.alert import Alert

# We just need to make sure the models are imported so SQLAlchemy can resolve the string references
